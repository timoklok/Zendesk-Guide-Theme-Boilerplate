<include src="hero-partial.hbs"></include>

<div class="container">

    <section class="section user-links">
<!--     <h2 class="visibility-hidden">{{ t 'categories' }}</h2> -->
    <section class="categories">

      <h2>
        Welkom Gebruiker, hier staat een tekst om mensen te introduceren in de omgeving. 
      </h2>
      <ul class="category-list">
      
        <li class="category-list__item">
          {{#link 'new_request' class='category-list__item-link button'}}Maak een nieuw ticket{{/link}}
        </li>

        <li class="category-list__item">
          {{#link "my_activities" class="category-list__item-link button"}}Bekijk al mijn tickets{{/link}}
        </li>
      
         
      </ul>
    </section>

  {{!-- <section class="categories">
    <ul class="category-list">
      {{#each categories}}
        {{#if ../has_multiple_categories}}
          <li class="category-list__item">
            <a href='{{url}}' class="category-list__item-link">
              <each loop="cat in categories">
                {{#is id {{% cat.id %}} }}
                  <img src="{{asset '{{% cat.icon %}}'}}" class="category-list__item-icon" />
                {{/is}}
              </each>
              <h4 class="category-list__item-title">{{name}}</h4>
              <p class="category-list__item-description">{{excerpt description}}</p>
            </a>
          </li>
        {{else}}
          {{#each sections}}
            <li class="category-list__item">
              <a href='{{url}}' class="category-list__item-link">
                <h4 class="category-list__item-title">
                  {{name}}
                </h4>
                <p class="category-list__item-description">{{excerpt description}}</p>
              </a>
            </li>
          {{/each}}
        {{/if}}
      {{/each}}
    </ul>
    {{pagination}}
  </section> --}}
</div>

<script src="{{asset 'home_page.js'}}"></script>
